Options -MultiViews
RewriteEngine On

# Se o arquivo/pasta existir, entrega normalmente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Caso contrário, manda tudo pro index.html (SPA)
RewriteRule ^ index.html [L]
